#include <iostream>
using namespace std;

int main(){
	int a = 1;
	double b = 2;
	cout << a / b << endl;
	return 0; // This is a sanity check.
	// By default, you want C++ to return 0 if the program runs without error.
}
